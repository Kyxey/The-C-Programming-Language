#define EXERCISE "1-7. Write a program to print the value of EOF."

#include <stdio.h>

int main()
{
    printf("✅\tExercise %s\n", EXERCISE);

    printf("The value of EOF is: %d\n", EOF);

    return 0;
}